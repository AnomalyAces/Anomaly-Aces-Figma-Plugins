{
  "name": "anomaly-aces-figma-exporter",
  "version": "1.0.0",
  "description": "Figma plugin to export components as individual SVGs with associated properties in JSON.",
  "main": "dist/code.js",
  "scripts": {
    "build": "node build.js",
    "watch": "node build.js --watch"
  },
  "dependencies": {
    "jszip": "^3.10.1"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.100.0",
    "esbuild": "^0.20.2",
    "typescript": "^5.4.5"
  }
}
